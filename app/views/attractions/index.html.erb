<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Media</th>
      <th>Weekday</th>
      <th>Date</th>
      <th>Hour</th>
    </tr>
  </thead>

  <tbody>
    <% @attractions.each do |attraction| %>
      <tr class="warning">
        <td><%= attraction.name %></td>
        <td><%= attraction.media %></td>
        <td><%= attraction.time.strftime("%A") %></td>
        <td><%= attraction.time.strftime("%d %B %Y") %></td>
        <td><%= attraction.time.strftime("%H:%M") %></td>
        <td><%= link_to 'Edit', edit_attraction_path(attraction), class: "btn btn-warning btn-xs" %></td>
        <td><%= link_to 'Delete', attraction, method: :delete, class: "btn btn-danger btn-xs", data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Attraction', new_attraction_path, class: "btn btn-primary" %>